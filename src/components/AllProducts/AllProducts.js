import { Fragment } from 'react';
import Hero from '../AllProducts/Hero';

import Logo from '../AllProducts/Logo';

const AllProducts = () => {
  return (
    <Fragment>
      <Logo />;
      <Hero />;
    </Fragment>
  );
};

export default AllProducts;
